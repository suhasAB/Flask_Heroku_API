@app.route('/is_valid_email/<email>')
def is_valid_email(email):
    return re.match(r"[^@]+@[^@]+\.[^@]+", email) != None
@app.route('/is_valid_email/<email>')
def is_valid_email(email):
    return re.match(r"[^@]+@[^@]+\.[^@]+", email) != None


@app.route('/is_valid_email/<email>')
def is_valid_email(email):
    return re.match(r"[^@]+@[^@]+\.[^@]+", email) != None

